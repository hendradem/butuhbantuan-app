name: Deploy Production

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # ================= BACKEND BUILD =================
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build Go binary
        run: |
          set -e
          cd services/backend
          go mod tidy
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o api ./cmd/api
          ls -lah api

      # ================= FRONTEND BUILD =================
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Next app (standalone)
        run: |
          set -e
          pnpm --filter next-app build
          cd apps/next-app
          ls -lah
          tar -czf next-build.tar.gz .next public
          ls -lah next-build.tar.gz

      # ================= PREPARE VPS =================
      - name: Prepare directories
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          script: |
            mkdir -p /var/www/butuhbantuan-app/backend
            mkdir -p /var/www/butuhbantuan-app/frontend

      # ================= UPLOAD BACKEND =================
      - name: Upload backend binary
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          source: services/backend/api
          target: /var/www/butuhbantuan-app/backend/api
          overwrite: true

      # ================= UPLOAD NEXT =================
      - name: Upload Next build archive
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          source: apps/next-app/next-build.tar.gz
          target: /var/www/butuhbantuan-app/frontend/next-build.tar.gz
          overwrite: true

      # ================= RESTART =================
      - name: Restart services
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          script: |
            set -e

            # Ensure backend executable
            chmod +x /var/www/butuhbantuan-app/backend/api

            # Extract frontend cleanly
            cd /var/www/butuhbantuan-app/frontend
            rm -rf .next
            tar -xzf next-build.tar.gz
            rm next-build.tar.gz

            # Backend (zero downtime)
            pm2 reload backend || pm2 start /var/www/butuhbantuan-app/backend/api --name backend

            # Frontend (zero downtime)
            pm2 reload next-app || pm2 start /var/www/butuhbantuan-app/frontend/.next/standalone/server.js --name next-app

            pm2 save
